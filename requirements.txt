numpy
opencv-python
psutil
insightface
face_recognition_models

# face_recognition depende do pacote "dlib", que no Windows so instala compilando
# com Visual Studio + CMake. Para evitar isso, use a wheel pre-compilada dlib-bin
# e instale o face_recognition sem puxar a dependencia real:
#   pip install dlib-bin
#   pip install --no-deps face_recognition
dlib-bin
face_recognition
